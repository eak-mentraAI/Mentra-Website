# Mentra Website Remediation Roadmap

**Created:** 2026-03-12
**Source:** [Codebase Audit](./audit.md)

---

## Sprint Overview

| Sprint | Theme | Duration | Spec |
|--------|-------|----------|------|
| **Sprint 1** | Security Hardening & Repo Hygiene | 1 day | [SPEC-001](./specs/SPEC-001-security-hardening.md) |
| **Sprint 2** | Dead Code Removal & Dependency Pruning | 1-2 days | [SPEC-002](./specs/SPEC-002-dead-code-removal.md) |
| **Sprint 3** | Code Quality & React Best Practices | 1-2 days | [SPEC-003](./specs/SPEC-003-code-quality.md) |
| **Sprint 4** | CI/CD Pipeline & Containerization | 1-2 days | [SPEC-004](./specs/SPEC-004-cicd-containerization.md) |
| **Sprint 5** | Repo Separation & Folder Restructure | 2-3 days | [SPEC-005](./specs/SPEC-005-repo-restructure.md) |

---

## Sprint 1 — Security Hardening & Repo Hygiene
**Spec:** [SPEC-001](./specs/SPEC-001-security-hardening.md)
**Status:** COMPLETE

### Tasks
- [x] Remove hardcoded credentials from `dashboard-refactored/config/constants.js`
- [x] Remove hardcoded API keys from `dashboard-refactored/config/constants.js`
- [x] Replace `exec()` with `execFile()` in `dashboard-refactored/server/services/vllm.js`
- [x] Remove hardcoded session secret fallback
- [x] Add `.env`, `.env.*`, and `.DS_Store` to `.gitignore`
- [x] Create `.env.example` documenting all required environment variables
- [x] Remove tracked `.DS_Store` files from git
- [x] Remove duplicate lock file (`bun.lockb`) — standardize on npm
- [x] Fix broken OG image / favicon paths in `index.html` and `404.html`

### Acceptance Criteria
- Zero secrets in source code
- `.env.example` exists with all required vars documented
- `exec()` replaced with `execFile()` everywhere
- `.gitignore` covers `.env*`, `.DS_Store`, and lock files for unused managers
- OG/favicon images resolve correctly

---

## Sprint 2 — Dead Code Removal & Dependency Pruning
**Spec:** [SPEC-002](./specs/SPEC-002-dead-code-removal.md)
**Status:** COMPLETE

### Tasks
- [x] Remove 46 unused UI components from `src/components/ui/`
- [x] Remove 7 unused custom modal components
- [x] Remove `src/components/OptimizedImage.tsx`
- [x] Remove `src/types/project.ts`
- [x] Remove `src/utils/evaluationUtils.ts`
- [x] Remove `src/utils/performance.ts`
- [x] Remove `src/components/ui/use-toast.ts` and `src/hooks/use-toast.ts` (radix toast chain)
- [x] Remove `src/hooks/use-mobile.tsx` (only used by deleted sidebar)
- [x] Remove unused dependencies from `package.json` (18 deps removed)
- [x] Remove `lovable-tagger` dev dependency and dead config in `vite.config.ts`
- [x] Remove stale `isAgentWorkbench` check from `App.tsx`
- [x] Remove duplicate toast system (removed radix Toaster, kept Sonner)
- [x] Remove unused `QueryClientProvider` wrapper
- [x] Simplify `sonner.tsx` to remove `next-themes` dependency
- [x] Remove empty directories (`src/types/`, `src/utils/`, `src/hooks/`)
- [x] Clean install and verify build succeeds

### Results
- `src/components/ui/` reduced from 68 files → 11 files
- 18 dependencies removed from `package.json`
- CSS: 87.79 KB → 51.52 KB (-41%)
- JS: 558.13 KB → 521.40 KB (-7%)
- Total bundle reduction: ~73 KB raw / ~16 KB gzip

---

## Sprint 3 — Code Quality & React Best Practices
**Spec:** [SPEC-003](./specs/SPEC-003-code-quality.md)
**Status:** COMPLETE

### Tasks
- [x] Add `ErrorBoundary` component wrapping the app (`src/components/layout/ErrorBoundary.tsx`)
- [x] Convert `<a>` tags to React Router `<Link>` in Header, Footer, HeroSection, CookieConsent, NotFound, HowItWorks
- [x] Add `React.lazy()` + `Suspense` code splitting for 12 page routes (Index kept eager)
- [x] Remove `dangerouslySetInnerHTML` from FAQ — replaced with `HighlightText` and `SectionHeader` components
- [x] Remove broken service worker (`public/sw.js`) and add unregister script for existing installations
- [x] Fix dead `/signup` link in HowItWorks → now points to `/pricing`

### Results
- Zero `dangerouslySetInnerHTML` in project source
- 12 lazy-loaded page chunks generated by Vite
- Main JS bundle: 521.40 KB → 382.89 KB (-27%)
- Total initial load (CSS + JS): ~434 KB → ~383 KB gzip: ~131 KB → ~122 KB
- Service worker fully removed with cleanup script for returning visitors

---

## Sprint 4 — CI/CD Pipeline & Containerization
**Spec:** [SPEC-004](./specs/SPEC-004-cicd-containerization.md)
**Status:** COMPLETE

### Tasks
- [x] Split CI into `ci.yml` (lint + typecheck + audit) and `deploy.yml`
- [x] Add `npm run lint` step to CI and deploy pipelines
- [x] Add `tsc --noEmit` typecheck step to CI and deploy pipelines
- [x] Add `npm audit --audit-level=high` step (with `continue-on-error` for non-blocking)
- [x] Add `node_modules` caching via `actions/setup-node` cache option
- [x] Create multi-stage `Dockerfile` (node:18-alpine build + nginx:1.25-alpine serve)
- [x] Create `docker-compose.yml` for local development
- [x] Create `.dockerignore` excluding node_modules, .git, docs, dashboard, etc.
- [x] Create `nginx.conf` with SPA fallback, asset caching, security headers, gzip
- [x] Fix 4 pre-existing lint errors (`prefer-const`, `no-explicit-any`, `no-require-imports`)
- [x] Convert tailwind.config.ts from `require()` to ESM `import`

### Results
- CI pipeline: lint (0 errors) + typecheck (clean) + audit + build
- Deploy pipeline: lint + typecheck + build + GitHub Pages deploy
- Both pipelines use npm caching for faster runs
- Dockerfile produces ~25 MB nginx-alpine container with health check
- nginx.conf provides proper SPA routing (eliminates need for 404.html hack in containerized deploys)
- Security headers: X-Frame-Options, X-Content-Type-Options, Referrer-Policy

---

## Sprint 5 — Repo Separation & Folder Restructure
**Spec:** [SPEC-005](./specs/SPEC-005-repo-restructure.md)
**Status:** COMPLETE

### Tasks
- [x] Remove `dashboard-refactored/` from this repository
- [x] Reorganize `src/components/` into `layout/`, `sections/`, `ui/`
- [x] Rename UUID-based image files to descriptive names (6 files × 2 formats = 12 renames)
- [x] Update all image references after rename (~20 files updated)
- [x] Remove `scripts/optimize-images.cjs`
- [x] Final folder structure matches audit recommendation
- [x] Update all import paths for moved components (14 files updated)
- [x] Verify lint (0 errors), typecheck (clean), and build succeeds

### Results
- `dashboard-refactored/` removed — repo now contains only the marketing site
- `src/components/` reorganized: `layout/` (3 files), `sections/` (7 files), `ui/` (11 files)
- 6 UUID image files renamed to descriptive names (both .png and .webp variants)
- `scripts/` directory removed (no longer needed)
- Build output unchanged: ~383 KB JS + ~51 KB CSS

### Acceptance Criteria
- This repo contains only the marketing site
- Component folders have clear, intuitive organization (`layout/`, `sections/`, `ui/`)
- All image files have human-readable names
